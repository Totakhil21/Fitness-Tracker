<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Summary</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Workout Summary</h1>
        <a class="summary-link" href="/">Add More Workouts</a>
        <ul>
            <% workouts.forEach(workout => { %>
                <li class="workout-entry">
                    <strong>Type:</strong> <%= workout.workoutType %> |
                    <strong>Duration:</strong> <%= workout.duration %> mins |
                    <strong>Intensity:</strong> <%= workout.intensity %> |
                    <strong>Date:</strong> <%= workout.date %> |
                    <strong>Notes:</strong> <%= workout.notes || "None" %>
                </li>
            <% }); %>
        </ul>
    </div>
</body>
</html>
